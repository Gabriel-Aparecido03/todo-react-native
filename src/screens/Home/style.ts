import { StyleSheet } from "react-native";
import { colorPallete } from "../../styles/colorPallete";

export const styles = StyleSheet.create({
  container : {
    backgroundColor : colorPallete.gray600,
    flex : 1,
  }
})